# Copy to .env for local development (astro dev picks this up automatically).
# In Netlify, set this in Site settings > Environment variables instead —
# never commit the real key.
ANTHROPIC_API_KEY=sk-ant-...
